# HANDOFF — EchoTracker

> Обновлён: 2026-09-08 вечером (локальное UTC+5)
> Причина: конец сессии по требованию, продолжение в новой сессии

## Текущая задача

Полигон в BikDesk переведён в режим без входа (подробности — HANDOFF.md в корне BikDesk). Апстрим-шаблон при этом не тронут режимом без входа.

## Что сделано в этой сессии (апстрим)

- Глазик вместо слов «Показать пароль» (поле пароля, свой SVG, фокус не теряется)
- Вход без двойных отправок (защита от гонки «вошёл + ошибка»)
- Честный лимит: считаются только неудачные попытки, удачный вход обнуляет; 429 по-русски
- DEV_AUTOLOGIN=1 — автовход только с 127.0.0.1/::1, по умолчанию 0
- Проверено: typecheck 2/2, web build
- Витрина проекта в шаблоне: картинки и HTML-строки в конвертере, короткий список доков с бейджем, BOARD_NAME в health + логотип в углу шапки, вставка скриншотов в карточку; вход не тронут
- Проверено: typecheck 2/2, web build, живой прогон со входом (форма, выход, доска с логотипом)
- Доки обновлены: README, CONTEXT, DECISIONS (шаблон — источник истины; проект называется сам), AGENTS

## Что осталось сделать

- Живой прогон Gmail-уведомлений — за пользователем
- Смена пароля в UI отсутствует
- Деплой echo на Sat (systemd + Caddy) — за пользователем
- Знание в GrimoireHub по трекеру — оформить когда взлетит

## Расхождение с полигоном (осознанно)

Копия в BikDesk/echo ушла дальше: вход и личности вырезаны целиком (сервер считает всех первым админом, клиент без формы/бейджей/авторов). Обратно в апстрим НЕ портировать без явной просьбы — шаблон должен оставаться со входом.

## Ключевые файлы

- api/src/server.ts — честный лимит, DEV_AUTOLOGIN, BOARD_NAME в health, фикс заголовков доков
- web/src/App.tsx, styles.css — глазик, защита входа, логотип проекта
- web/src/Docs.tsx, md.ts — короткий список, картинки и HTML-строки
- web/src/CardModal.tsx — вставка скриншотов

## Команды для проверки

```bash
pnpm typecheck
pnpm --filter @echotracker/web build
```

## Следующий шаг

Продолжить по HANDOFF.md из BikDesk; в апстрим без просьбы ничего не тащить.
